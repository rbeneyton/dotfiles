# wallpaper from awesome data dir (awesome package still deployed)
# hyprpaper 0.8+ block format; empty monitor = fallback for all monitors

wallpaper {
    monitor =
    path = {{trim (command_output "realpath ~")}}/.config/awesome/data/35471812.jpg
    fit_mode = cover
}
